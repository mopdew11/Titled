/// @description 
draw_sprite(spr_items,ID,x,y);

draw_set_color(c_yellow);


if(sign(x-obj_player.x) = 1)
{
	
	draw_text(x + drawAdd, y- drawAdd - 4, drawE);
}

else draw_text(x - drawAdd - 8, y-drawAdd - 4, drawE);
